header.header
    nav.nav.nav--plots
        a.nav__el(href='/') All plots
    .header__logo
        img(src='/img/logo.png' alt='OPG logo')
    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout Log Out
            a.nav__el(href='#')
                img.nav__user-img(src='/img/users/user-2.jpg' alt=`Photo of ${user.name}`)
                span= user.name.split(' ')[0]
        else
            a.nav__el(href='/login') Log in
            a.nav__el.nav__el-cta(href='/signup') Sign up